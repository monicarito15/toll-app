import SwiftUI

struct VehicleView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("VehicleViewe")
        }
        .padding()
    }
}

#Preview {
    VehicleView()
}

